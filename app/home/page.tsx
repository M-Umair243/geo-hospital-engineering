import HeroCarousel from "@/components/hero-carousel";
import ProductsCards from "./products-card";

const page = () => {
  return (
    <div>
      <HeroCarousel />
      <ProductsCards />
    </div>
  );
};

export default page;
